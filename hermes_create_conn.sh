#!/bin/bash

hermes --config ~/.hermes/config.toml create connection --a-chain  testing  --b-chain  testing2
#hermes --config ~/.hermes/config.toml create channel --a-chain  testing  --a-connection connection-0 --a-port transfer --b-port transfer


